import LoginButton from '../LoginButton/LoginButton'

const Lobby = () => {
  return (
    <div>
      <h2>{'Lobby'}</h2>
      <p>{'Find me in ./web/src/components/Lobby/Lobby.tsx'}</p>
      <p>
        <LoginButton />
      </p>
    </div>
  )
}

export default Lobby
